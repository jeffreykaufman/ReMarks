'use strict';

/**
 * Creates a new instance of the `PeekRequest` class.
 *
 * @class
 * @param {number} distance
 */
function PeekRequest(distance) {
    this.distance = distance;
}

module.exports = PeekRequest;
